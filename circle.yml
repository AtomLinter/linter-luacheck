dependencies:
  override:
    - curl -o atom-amd64.deb https://atom.io/download/deb
    - sudo dpkg --install atom-amd64.deb || true
    - sudo apt-get -f install
    - sudo apt-get install lua5.1 luarocks
    - apm install
test:
  override:
    - apm test
